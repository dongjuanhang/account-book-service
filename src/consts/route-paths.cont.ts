// 账目分类
export enum CATEGORIES_PATH {
  ROOT = 'categories',
  CODE_PARAM = 'categoryCode',
}

// 账目
export enum TRANSACTIONS_PATH {
  ROOT = 'transactions',
  CODE_PARAM = 'transactionCode',
}

// 用户
export enum USERS_PATH {
  ROOT = 'users',
  ACCESS = 'access',
}
